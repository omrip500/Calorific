<resources>

    <!-- Base application theme -->
    <style name="Theme.YourAppName" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/dark_green</item>
        <item name="colorPrimaryVariant">@color/dark_green_variant</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/light_green</item>
        <item name="colorSecondaryVariant">@color/light_green_variant</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Button background color -->
        <item name="colorButtonNormal">@color/dark_green</item>
        <item name="buttonStyle">@style/CustomButtonStyle</item> <!-- סגנון ברירת מחדל לכפתורים -->

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/dark_green_variant</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Customize your theme here -->
        <item name="android:colorBackground">@color/light_gray</item>
        <item name="android:textColorPrimary">@color/dark_gray</item>
    </style>

    <!-- Custom button style -->
    <style name="CustomButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/green</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>
